#include <iostream>
#include <string>
namespace randomBin
{
    /* 
        author:nnk
        para:
            @cmd:输入在命令行中的指令 eg: python main.py
            @num:图片序列编号
        func:生成单个随机640*480的bin文件
     */
    void createRandomBin (std::string cmd,int num);
}